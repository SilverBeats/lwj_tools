存放一些我平常使用过的代码，封装好的。
这些代码会存在不足和 Bug，欢迎留言，我视心情修复或完善。

# Install

```shell
pip install .

# 若是使用 lwj_tools.utils.model.py 的代码
pip install ".[model]"

# 若是使用 lwj_tools.llms 中的代码
pip install ".[llms]"

# 若是使用 lwj_tools.evaluators.nlg 中的代码
pip install ".[nlgeval]"

# 若是使用所有代码
pip install ".[all]"
```
